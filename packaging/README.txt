WinBoat Helper
===============

Install for the current user from the extracted release bundle:

  ./install-user.sh

Uninstall later:

  ~/.local/lib/winboat-helper/uninstall-user.sh

After install, launch from your app menu or run:

  ~/.local/bin/winboat-helper
